// This component is no longer used.
// Keeping an empty export to prevent build errors if referenced elsewhere by mistake.
import React from 'react';

export const AICoach: React.FC<any> = () => {
  return null;
};